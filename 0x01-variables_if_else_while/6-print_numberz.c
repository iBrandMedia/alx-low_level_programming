#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)

{
	int C;

	C = 0;
	while
		(C < 10) {
			putchar(C + '0');
			C++;
		}
	putchar('\n');
	return (0);
}
