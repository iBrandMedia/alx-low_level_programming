#include <stdio.h>
/**
 * main - Prints the lowercase alphabet in reverse, followed by new line
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
	char b;

	b = 'z';
	while
		(b >= 'a') {
			putchar(b);
			b--;
		}
	putchar('\n');
	return (0);
}

