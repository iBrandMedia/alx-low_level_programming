#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char C;

	C = 'Z';
	while
		(C >= 'a') {
			putchar(C);
			C--;
		}
	putchar('\n');
	return (0);
}

